<!DOCTYPE html>
<html>
  <head>
    <title>MadLib</title>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
  </head>
  <body>
    <h1>Travel</h1>
    <p>@Model.GetName() lost in the forest. In the deep of the forest saw a @Model.GetAdjective() house and strange voices like a  @Model.GetExclamation(). The weather was cloudy and dark, like a dark @Model.GetSeason() days</p>
  </body>
</html>
